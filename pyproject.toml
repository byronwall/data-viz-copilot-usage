[project]
name = "data-viz-copilot-usage"
version = "0.1.0"
description = "Interactive viewer for Copilot and Codex coding-session token usage"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0",
]

[tool.uv]
package = false
